{
  "name": "howdy-v2",
  "version": "2.0.0",
  "description": "Howdy, is simple weather app",
  "default": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server",
    "watch": "parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "MohammadAli Sedaghat",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "parcel": "^2.0.0-rc.0"
  }
}
